name: Documentation

on:
  push:
    branches: ["**"]
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - 'SECURITY_CONTROLS_INSTALLATION.md'
      - 'SECURITY_CONTROLS_ARCHITECTURE.md'
      - 'YUBIKEY_SIGSTORE_GUIDE.md'
      - 'docs/security/**'
  pull_request:
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - 'SECURITY_CONTROLS_INSTALLATION.md'
      - 'SECURITY_CONTROLS_ARCHITECTURE.md'
      - 'YUBIKEY_SIGSTORE_GUIDE.md'
      - 'docs/security/**'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

      - name: Build MkDocs site
        run: |
          set -euo pipefail
          python3 -m pip install --upgrade pip
          python3 -m pip install mkdocs mkdocs-material
          mkdocs build -d site

      - name: Check links with lychee
        uses: lycheeverse/lychee-action@885c65f3dc543b57c898c8099f4e08c8afd178a2 # v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: >-
            --config lychee.toml
            docs/**/*.md README.md SECURITY_CONTROLS_INSTALLATION.md SECURITY_CONTROLS_ARCHITECTURE.md docs/security/*.md

      - name: Upload Pages artifact
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa # v3
        with:
          path: site

  deploy:
    # Only deploy from main branch
    if: github.ref == 'refs/heads/main'
    needs: build-and-test
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4